= form_for @post do |f|
  = render 'shared/error_messages', object: f.object

  = f.label :title
  = f.text_field :title

  = f.label :text
  = f.text_area :text

  = f.submit submit_button, class: "btn btn-primary btn-large"